import { FC } from "react";
import CardFrame from "./CardFrame";

interface PageProps {
};

const User: FC<PageProps> = ({}) => {
  return (
    <div>
      <CardFrame className="">
        asdfkjnasdjk
      </CardFrame>
    </div>
  );
};

export default User;